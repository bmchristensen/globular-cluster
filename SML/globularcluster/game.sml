(* Game *)

fun gameLoop(planet::remainingList) =
    planet;

fun start() =
    (* Call function to display title screen *)
    (* Wait for user input *)
    let
        val planets = [p1, p2, p3, final]
    in
        gameLoop(planets)
    end;
